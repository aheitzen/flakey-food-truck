$(document).ready(function(){
	console.log("READY");
	$('.slider').slick({ 
		// infinite: true,
		// slidesToShow: 1,
		// slidesToScroll: 1,
		// variableWidth: false,
		// dots: true,
		// arrows: true,
		autoplay: true
	});
});
